package dev.tarna.moretweaks.api.utils

import org.bukkit.Material
import org.bukkit.Tag

val allSaplings = Tag.SAPLINGS.values.toList()

val allLogs = Tag.LOGS.values.toList()

val allPlanks = Tag.PLANKS.values.toList()

val allWool = Tag.WOOL.values.toList()

val allFarmLandBlocks = listOf(
    Material.FARMLAND,
    Material.DIRT,
    Material.GRASS_BLOCK,
    Material.COARSE_DIRT,
    Material.PODZOL,
    Material.MYCELIUM
)